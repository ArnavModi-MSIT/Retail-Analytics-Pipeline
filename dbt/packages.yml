# dbt's equivalent of requirements.txt — installed with `dbt deps` into
# dbt_packages/ (gitignored, never committed, just like node_modules).
packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.3.0", "<2.0.0"]
